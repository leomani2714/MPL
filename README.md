# MPL

## Multiplayer server

```bash
npm install
npm start
```

Open `http://localhost:3000` in two browser windows. Click `+` in the first window to create a room, then enter the displayed six-character code in the second window and click the join arrow. The host window owns the match state; both players can answer from their own browser during their innings.